fun main() {
    println("My Code is running") // This prints a message to the console

    // This is a single-line comment

    /*
       This is a multi-line comment
       Useful for explaining longer blocks of code
    */

    /*
       Important Note:
       - When you use `var`, the variable's value can change.
       - When you use `val`, the value cannot be changed.
       - In Kotlin, you create variables using `var` or `val`:

         var variableName = value
         val constantName = value

       - The data type is automatically inferred by Kotlin:
         var number = 1           // Integer
         var text = "stringname"  // String

       - To print a variable, use:
         println(variableName)
    */

    // Call the function defined below
    VariableType()
}

